export function PostDetailCommentSection() {
  return (
    <div>
      <h2 className="mb-4 text-sm font-medium text-text-primary">댓글</h2>

      {/* TODO: 여기 CommentInput / CommentList 붙이면 됨 */}
      <div className="rounded-lg border border-border-default p-4 text-sm text-text-muted">
        댓글 영역 (연결 예정)
      </div>
    </div>
  )
}
